<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        {{postagem.conteudo}}
        <hr>
        <small>Data de publicação: {{#formatDate postagem.date}}{{/formatDate}}</small><br>
    </div>
</div>