<h2>Categorias: </h2>
<hr>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body mt-05" >
            <a href="/categorias/{{_id}}"><h4>{{nome}}</h4></a>
            <small>Slug: {{slug}}</small><br>
            <small>Data de criação: {{#formatDate date}}{{/formatDate}} </small><br>
        
        </div>
              
    </div>

{{else}}
    <h4 class="mt-3">Nenhuma categoria registrada!</h4>
{{/each}}

